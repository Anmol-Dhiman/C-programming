#include <stdio.h>
int main()
{
    long long n;
    scanf("%lld", &n);
    long long a[n];
    long long long x[100000][1000000000], y[100000][1000000000];
    for (int i = 0; i < n; i++)
    {
        scanf("%lld", &a[i]);
        for (int j = 0; j < a[i]; j++)
        {
        }
    }

    return 0;
}